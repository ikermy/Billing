# Preserve yarn-like peer-dependency resolution behavior.
# @nestjs/swagger@11 requires @nestjs/common@^11, but the project targets v10.
# Remove this flag when all @nestjs/* packages are upgraded to v11.
legacy-peer-deps=true

